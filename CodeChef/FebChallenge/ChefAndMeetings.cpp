#include<bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    int t;
    cin >> t;
    while(t--) {
        string p;
        gets(p);
        // cin >> p;
        cout << "Heee" << endl;
        int n;
        cout << "h222";
        // cin.clear();
        // fflush(stdin);
        cin >> n;
        string out = "";

        // vector<pair<string, string>> arr(n);
        for (int i = 0; i < n; i++) {
            string l, t;
            gets(l);
            gets(t);

            cout << l.substr(0, 2) << endl;
            // arr.push_back()
        }
        
    }
    return 0;
}
