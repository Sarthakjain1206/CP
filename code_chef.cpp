#include <bits/stdc++.h>
using namespace std;

int gcd(int a, int b) {
    if(a == 0) return b;
    return gcd(b % a, a);
}

int main(int argc, char const *argv[])
{
    int t;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;
        while(--n) {
            int x, y, z;
            cin >> x >> y >> z;
            unordered_set <int> s;
            

        }
    }
    return 0;
}
